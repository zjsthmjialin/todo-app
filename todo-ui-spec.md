# Todo 应用 UI 设计规范

> 提取自 TODO01 版 React 原型，供 PyQt5 桌面版参考

---

## 一、颜色体系

### 1. 主色板

| 用途 | 颜色名 | 色值 | 说明 |
|------|--------|------|------|
| 主强调色 | 森林绿 | `#5a8f6e` | 按钮、选中态、勾选框、进度百分比 |
| 主强调浅 | 薄荷绿 | `#7ab896` | 进度条渐变起点 |
| 主强调极浅 | 翠雾 | `#a8d5b8` | 进度条渐变终点 |
| 主强调透明 | 绿雾 | `rgba(122,158,135,0.12)` | 日期标签背景 |
| 主强调阴影 | 绿影 | `rgba(90,143,110,0.25)` | 添加按钮阴影 |

### 2. 中性色板

| 用途 | 色值 | 说明 |
|------|------|------|
| 深棕（标题） | `#2c2418` | 主标题文字 |
| 深褐（正文） | `#3c3020` | 任务文字 |
| 中灰褐 | `#a09080` | 副标题、引言、标签文字 |
| 浅灰褐 | `#b0a898` | 已完成删除线文字、placeholder |
| 暖灰 | `#c8bfb0` | 未勾选框边框、删除按钮、空状态图标 |
| 极浅灰 | `#c0b0a0` | 进度小字 |
| 暖白 | `#ddd6c8` | 输入框边框、清除按钮边框 |
| 米白 | `#e8e0d4` | 任务项边框 |

### 3. 背景色板

| 用途 | 色值 | 说明 |
|------|------|------|
| 页面底色 | `#f0ebe0` | 整体背景 |
| 页面渐变1 | `#e8f4ee` | 左上角微光（20% 10%位置，50%透明衰减） |
| 页面渐变2 | `#fdf0e0` | 右下角暖光（80% 90%位置，50%透明衰减） |
| 卡片底色 | `#fffdf8` | 主卡片背景 |
| 卡片边框 | `#e4ddd0` | 卡片1px边框 |
| 卡片阴影 | `rgba(100,80,50,0.10)` | 卡片投影 |
| 卡片内发光 | `rgba(255,255,255,0.9)` | 卡片顶部内发光 |
| 输入框底色 | `#f7f3ec` | 输入框、任务项背景 |
| 标签栏底色 | `#f0ebe0` | 筛选标签背景 |
| 进度条底色 | `#ede7dc` | 进度轨道背景 |
| 分割线 | `#ede7dc` | 底部分割线 |

### 4. 交互态

| 状态 | 色值 | 说明 |
|------|------|------|
| 输入框聚焦边框 | `#7a9e87` | focus 时边框变绿 |
| 输入框聚焦光晕 | `rgba(122,158,135,0.15)` | focus 时3px外发光 |
| 勾选完成背景 | `#5a8f6e` | 白色对勾+绿底 |
| 标签选中底色 | `#fffdf8` | 与卡片底色一致 |
| 标签选中文字 | `#5a8f6e` | 变为主强调色 |
| 任务完成态 | 透明度0.5 | 整行半透明 |
| 任务删除动画 | 向右偏移12px+透明→0 | 300ms退出 |

---

## 二、模块布局

### 1. 整体结构（自上而下）

```
┌─────────────────────────────┐
│         Header 区域          │
│  ┌──────────────────────┐   │
│  │  日期标签（绿色胶囊）  │   │
│  │  "任务清单"（标题）    │   │
│  │  随机引言（斜体灰字）  │   │
│  └──────────────────────┘   │
│                             │
│       Progress 区域         │
│  ┌──────────────────────┐   │
│  │  完成进度    67%      │   │
│  │  ████████░░░░░░░░░░  │   │
│  │  2 / 3 项任务完成     │   │
│  └──────────────────────┘   │
│                             │
│        Input 区域           │
│  ┌──────────────────┬───┐   │
│  │  添加新任务…      │ + │   │
│  └──────────────────┴───┘   │
│                             │
│       Filter Tabs           │
│  ┌──────┬──────┬──────┐    │
│  │ 全部 │进行中│已完成 │    │
│  └──────┴──────┴──────┘    │
│                             │
│       Todo List             │
│  ┌─────────────────────┐   │
│  │ ○ 阅读三十页书    ✕ │   │
│  │ ● 完成晨间锻炼    ✕ │   │
│  └─────────────────────┘   │
│                             │
│        Footer               │
│         清除已完成 (2) >     │
└─────────────────────────────┘
```

### 2. 卡片参数

| 属性 | 值 |
|------|-----|
| 最大宽度 | 460px |
| 圆角 | 20px |
| 内边距 | 36px 32px 28px |
| 外边距 | 32px 16px（页面居中） |

### 3. 各模块间距

| 区域 | 底部间距 |
|------|----------|
| Header | 28px |
| Progress | 28px |
| Input | 20px |
| Tabs | 20px |
| Footer 顶部分割线 | 16px 上边距 + 20px |

### 4. 子元素规格

#### 日期标签
- 背景：`rgba(122,158,135,0.12)` 胶囊形
- 文字：11px / 500 / `#5a8f6e` / 大写字母间距0.1em
- 内边距：4px 12px / 圆角20px

#### 标题
- 字体：Noto Serif SC（衬线体）
- 大小：30px / 600 / `#2c2418`

#### 进度条
- 轨道高度：5px / 圆角4px / 底色`#ede7dc`
- 填充渐变：`#7ab896` → `#a8d5b8` / 圆角4px
- 百分比数字：18px / 600 / `#5a8f6e` / 衬线体

#### 输入框
- 圆角12px / 内边距12px 16px
- 底色`#f7f3ec` / 边框1px `#ddd6c8`
- 文字：14px

#### 添加按钮
- 46×46px / 圆角12px / 绿底`#5a8f6e` / 白色加号图标
- 阴影：`0 2px 8px rgba(90,143,110,0.25)`

#### 筛选标签栏
- 底色`#f0ebe0` / 圆角10px / 内边距4px
- 未选中：12px / `#a09080`
- 选中：白底`#fffdf8` / 绿字`#5a8f6e` / 500粗 / 阴影`0 1px 4px rgba(100,80,50,0.08)`

#### 任务项
- 圆角12px / 内边距13px 14px
- 底色`#f7f3ec` / 边框1px `#e8e0d4`
- 勾选框：22×22px / 圆角6px / 边框1.5px `#c8bfb0`
- 勾选完成：绿底`#5a8f6e` + 白色对勾SVG 12px
- 文字：14px / 300 / `#3c3020`
- 已完成文字：删除线 + `#b0a898`
- 删除按钮：14px × 图标 / `#c8bfb0`

#### 清除按钮
- 12px / `#a09080` / 内边距6px 12px / 圆角8px
- 边框1px `#ddd6c8` / 白底

---

## 三、动效规范

| 动效 | 参数 |
|------|------|
| 任务出现 | `slideIn`：从下8px淡入，0.25s ease |
| 进度条填充 | `width 0.5s cubic-bezier(0.4,0,0.2,1)` |
| 任务删除 | 向右12px + 淡出，300ms |
| 输入框聚焦 | 边框色+光晕，0.2s |
| 按钮悬停 | 背景+位移，0.2s/0.1s |
| 完成态 | 整行透明度0.5 |

---

## 四、字体规范

| 用途 | 字体 | 备选 |
|------|------|------|
| 正文/按钮 | Noto Sans SC | 系统无衬线体 |
| 标题/百分比 | Noto Serif SC | 系统衬线体 |

| 层级 | 大小 | 粗细 |
|------|------|------|
| 页面标题 | 30px | 600 |
| 进度百分比 | 18px | 600 |
| 正文/输入 | 14px | 300-400 |
| 筛选标签 | 12px | 400-500 |
| 日期/辅助 | 11px | 500 |

---

## 五、特殊效果

1. **纸张纹理**：SVG fractalNoise 背景叠加，透明度0.6，200px平铺
2. **渐变微光**：左上角绿色光晕 + 右下角暖黄光晕
3. **卡片内发光**：顶部1px白色内阴影，模拟纸张边缘
4. **翻页折角**：任务清单图标右下角的纸张翻卷效果（视觉装饰）
